# 个人参数（脱敏：绝不进仓库）
config.json

# 个人交易台账与账户状态（**绝不进仓库**：含真实成交、成本、资金）
# ⚠️ 目录名带下划线前缀，避免与脚本 journal.py 撞名（见 pitfalls P48）
_journal/
journal/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# 报告产物 / 数据缓存（不要进仓库）
*.html
*.pdf
_kcache/
*.json.tmp
_nextday_*.json

# 一次性临时脚本（约定以 _ 开头，用完即删；防手滑提交）
scripts/_*.py

# 单次分析的人工研判输入（与生成的 HTML 报告成对，属产物不进仓库）
*_notes.json

# 本地视觉验收产物（把 PDF 渲染成逐页 PNG 看图用；见 pitfalls P61）
# ⚠️ 教训：这类目录若不显式忽略，git add -A 会把它们一起提交（本次曾误提交 13 张 PNG）
_pdf_*/
_v_*/
_pdfpng/

# 编辑器 / 系统
.vscode/
.idea/
.DS_Store
Thumbs.db
*.swp
_out_*.txt
pool.json
pool_deep.json
scripts/pool.json
scripts/pool_deep.json

# 隐私闸门禁词清单（**不入仓库**：写进去等于把禁词又公开一次）
_privacy_terms.txt
