# 选股分析牛马工具

一个**自包含、零依赖**的 Agent Skill：把 A 股隔日超短（当日买 / 次日卖）的研究流程、
数据源接口、判读铁律与回测方法固化下来，可被任何支持 `SKILL.md` 规范的 Agent 软件加载。

> 名字来源：干活的牛马自己给自己写工具。**不装任何 pip 包**，纯标准库跑通全链路。

**仓库地址**：https://github.com/Y5H1ggs/niuma-stock-screener
**当前版本**：`v1.3.0`（2026-09-22）　·　许可 MIT　·　完整版本沿革见 [`CHANGELOG.md`](CHANGELOG.md)

---

## 项目亮点

| # | 亮点 | 说明 |
|---|---|---|
| 1 | **零依赖** | 纯 Python 标准库（`urllib` / `json` / `ssl` / `re` / `math`），**不装任何 pip 包**，`git clone` 完直接跑 |
| 2 | **只给概率，不给指令** | 「禁止绝对化表述、所有情景必须带概率、≥50% 标高概率」是写进 `SKILL.md` 的**硬约束**，不是风格建议。工具输出候选池 + 情景概率，**不下买入信号** |
| 3 | **判读铁律固化** | 把散户最容易漏的盲区做成流程步骤：板块内资金接力偏好必查、连板梯队真实 streak 扫描、托单被动消耗识别、竞价三段式校准 |
| 4 | **多源降级链 + 不静默出错** | 东财 / 腾讯 / 新浪三源互为备份；**任一维度取数失败按中性分计入**并在报告打「数据完整度」警示——绝不把"取不到"当成"资金流出"来扣分 |
| 5 | **一键出报告** | `report.py` 一条命令产出卡片式 HTML 深度分析：**抬头关键指标格**（评级 / 当日主力 / 相对强度 / 250 日位置）+ 四维评级（技术/资金/板块/情绪）+ **本期点评**（机械指出最弱维度）+ **逐条 ±N 加减分理由** + **5 张 SVG 图表** + 板块横向对比 + 形态回测 + 自证校验结果 |
| 6 | **报告可脱敏、可导出** | `--anon` 一键生成**不含账户资金与持仓口径**的版本（发他人时用，从源头不读取而非截图裁剪）；`--pdf` 用本机 Chrome 直接导出 PDF，带 `@media print` 样式保证深底配色不被打印丢弃 |
| 6 | **个人参数全外置** | 资金 / 交易权限 / 费用率 / 止损纪律全在 `config.json`，**脚本里零硬编码**；首次运行有录入闸门，不会拿别人的默认值硬跑 |
| 7 | **57 条真实踩坑记录（P1~P57）** | 接口字段失真、单位差 100 倍、分钟级资金流是当日累计值（**禁止累加**）、占位符被静默读成 0、Chrome 不认相对路径……`references/pitfalls.md` 是一份别人已经踩过的坑清单 |
| 8 | **回测方法论文档化** | 形态回测的口径、必经输出、分档切分写在 `references/backtest.md`，并强制标注样本量——避免"样本 5 个就敢下结论" |
| 9 | **版式可复现** | 报告用墨蓝 + 金的高级感配色，每个小板块独立成卡；版式坑（父子卡边框打架、子卡贴边）已写入踩坑文档 |
| 10 | **基准不可挑选** | 相对强度不用"最热的板块"也不用"最细的同业"（两者都是**挑**出来的最有利解释），而是**行业基准（东财口径，每票恰一个）+ 题材基准（所属概念里资金最大者）双轨、取更不利者**，分歧时显式标注。同一只票换基准能差出 +0.53pp 与 −1.18pp 两个相反结论，这条规则就是为堵这个洞写的 |
| 11 | **板块对比前先校验主营** | 报告固定输出「主营构成（按产品）」，并在第 1 大主营 ≥50% 且与板块基准不同源时自动警示——防止把"题材标签"当成"业绩支撑"（某科技股主营 81% 是教育，却一直被拿科技板块做对比） |
| 12 | **买入前准入清单** | 10 条机械可判的逐项打勾（板块权限 / 价格上限 / 能否成交 / 连板高度 / 情绪温度 / 相对强度 / 止损位可算…），缺一条即不介入，避免硬约束散落各处被人肉记忆漏掉 |
| 13 | **PIT 时点回测** | 用财报**实际公告日**做可见性闸门：某日只能看到"公告日 ≤ 该日"的财报。并附 `pit.py --demo` 把"用最新财报回填历史"的偏差**量化**出来——不靠嘴说"前视偏差很重要" |
| 14 | **账户级风控闸门** | 从"单笔纪律"升级到"账户约束"：回撤熔断 / 行业敞口（禁止连续押同一条线）/ 市值档 / 风险预算。`risk.py check <代码>` 直接回答 **"这笔允许开吗、能开多少股"** |
| 15 | **校准闭环（能自我修正）** | 交易台账（append-only）+ 校准报告：真实胜率/盈亏比归因、**用 K 线重建"该止损未止损"的漏单**、预测概率校准与 Brier 分数。配 `references/calibration.md` 裁判规则：**样本 <30 笔不许调参 · 一次只改一个变量 · 改动必须留痕** |
| 16 | **交叉校验 + 置信度自检** | 同指标双源比对（腾讯/东财）、K 线三源、板块资金两口径；`health.py` 逐接口体检并给出**"今天这份结论的置信度"**——资金面与板块面同时挂掉时判定"应不予评级" |

---

## 适用人群

**适合你，如果：**

- 做 A 股 **T+1 隔日超短**（当日买 / 次日卖），需要一套可复用的研究流程；
- **只做主板**、没有创业板/科创板权限（默认就是主板过滤）；
- 偏好**全仓单吊**、不做分散（`position_mode` 可改成分散）；
- 想用 Agent 当研究助理，但**不想被喂结论**——你要的是数据和概率，不是"老师推荐"；
- 想学 A 股短线判读方法：`references/` 下四份文档可以脱离代码独立当手册读。

**不适合你，如果：**

- 做**中长线基本面 / 价值投资**——本工具是隔日超短口径，基本不碰估值与财报深度；
- 需要 **Level-2 逐笔 / 千档行情**或高频策略——只用公开免费接口；
- 想要"推荐明天买哪只"——它给的是候选池 + 概率 + 情景树，**不是买入信号**；
- 有创业板/科创板权限并想做那些板块——默认被过滤，需自行改 `config.json`；
- 期待开箱即用的实盘接单系统——这是**研究工具**，不含下单与账户对接。

---

## 已知局限（先说清楚）

> 写在亮点后面的诚意：下面是这个项目**目前做不到**的事，请据此判断它是否适合你。

| # | 局限 | 影响 |
|---|---|---|
| 1 | **回测样本量小** | 形态回测依赖个股历史同类形态，常见 N=5~15；**N<10 不可外推**，报告强制标注 N |
| 2 | **PIT 只管"能不能看到"，不管"数据被改过"** | 已用**财报实际公告日**做可见性闸门（某日只能看到公告日 ≤ 该日的财报），从根上消除前视偏差。但它**不处理追溯重述**——财报若被事后调整，历史时点读到的仍是原始披露值。做严谨研究时须自行核对修订公告 |
| 3 | **资金流是推断值，不是真值** | 主力/超大单按成交单金额分档推断，**拆单会系统性干扰**（大单拆小、小单凑大都误判） |
| 4 | **盘中快照会变** | 所有数据是取数那一刻的实时快照，收盘前结论可能变化，**用结论前必须复核** |
| 5 | **免费接口会限流** | 东财部分接口会临时返回空值，已有三级降级链，但**限流期间的维度没有信息量** |
| 6 | **账户级风控是机械规则，它不判断市况** | 已有回撤熔断 / 行业敞口 / 市值档 / 风险预算四道闸门，但闸门只做**机械约束**：它不知道今天该不该出手。熔断能防连续失血，**不能替你决定方向** |
| 7 | **没有经审计的实盘业绩** | 这是个人研究工具，不提供业绩承诺；下方路线图是**计划，不是承诺** |
| 8 | **评级是机械打分** | 四维加总模型输出，非券商研报；权重与阈值是经验设定，未做参数优化 |
| 9 | **双基准消除了"挑基准"，但不消除分类误差** | 行业取东财口径、题材取"资金最大者"，只保证**基准不由分析者挑选**；板块成员归属本身仍有误差（同一票在不同板块里排名可能差异明显），故报告同时印出两个基准，请自行交叉判断 |
| 10 | **主营构成有报告期滞后** | 取自最新披露期（如半年报），**当期业务变化不会立刻体现**；它用于区分"题材 vs 主业"，不能替代最新财报阅读 |
| 11 | **校准需要足够样本才有意义** | 参数调优的门槛是 **30 笔已平仓交易**（30 笔下真实胜率 50% 的 95% 置信区间约 **32%~68%**）。样本不足时工具**拒绝给调参建议**——这是刻意设计，不是缺陷 |
| 12 | **预测概率是"该股自身形态的基准分布"** | `predict.py` 的输出**不含**板块 / 资金 / 情绪修正，必须叠加三层调整后才可用；形态邻域样本通常只有 N=15~30，跨市况适用性下降 |
| 13 | **未接入 AI 代码审查** | 本项目的主要风险是**业务口径类 bug**（比较错字段、接口占位符被读成 0），这类"语法正确、逻辑自洽"的错误通用审查工具抓不到，只能靠跨源校验与自证关系（如"主力净额必须 == 大单 + 超大单"）兜底 |

---

## 一、这个 Skill 包含什么

| 文件 | 内容 |
|---|---|
| `SKILL.md` | **主入口**：自升级与校准协议 · 基础数据录入 · 硬约束 · 执行流程 · 三口径切换 · 竞价校准 · 输出格式 |
| `CHANGELOG.md` | **版本沿革**：每次升级的依据、改前→改后，以及"**这一期为什么没改**" |
| `config.example.json` | **个人参数模板**（资金 / 交易权限 / 费用率 / 止损纪律 / 账户级风控）——复制为 `config.json` 后填写 |
| `references/datasources.md` | 全部数据源接口 URL + 字段索引 + 限流降级链 + **跨源量纲对照表** |
| `references/playbook.md` | **判读铁律（12 条）**：板块资金接力、情绪温度、相对强度、托单消耗、封单强度、竞价、脉冲识别、次日概率三口径 |
| `references/backtest.md` | T+1 形态回测方法论（口径 / 必经输出 / 分档切分） |
| `references/pitfalls.md` | **57 条真实踩坑记录（P1~P57）**：接口字段失真、单位差 100 倍、分钟级资金流是当日累计值（**禁止累加**）、占位符被读成 0、相对路径不生效…… |
| `references/calibration.md` | **校准与参数调优裁判规则**：什么证据强度才允许改参数 · 禁止行为清单 |
| `scripts/cfg.py` | **个人参数配置加载 + 首次录入闸门**（含市场档与账户级风控参数） |
| `scripts/ds.py` | **便携数据源库**（纯标准库，零依赖）：快照 / 列表 / 资金流 / 板块 / K线三源 / 情绪 / **财务 PIT** / 全套技术指标 |
| `scripts/scan_pool.py` | 全市场候选池扫描（强势 / 趋势 / 潜力 三口径，内置**可买入闸门**自动剔除涨停） |
| `scripts/sector_compare.py` | 板块内横向对比（铁律 1 + 相对强度，含属性桶过滤） |
| `scripts/mood.py` | 情绪温度三指标（昨日涨停系资金 + 真实连板梯队 + 主线错位） |
| `scripts/backtest.py` | T+1 形态回测（12 种内置形态 + 基准对照 + 极端案例明细） |
| `scripts/patterns.py` | 形态库扩展（22 条，四族）+ **一行 DSL 自定义形态**（`--rule "close>ma5 and rsi14<40"`） |
| `scripts/predict.py` | **次日涨跌概率预测**（形态邻域回测；开 / 收 / 盘中**三口径分离**，见铁律 12） |
| `scripts/report.py` | **一键生成卡片式 HTML 深度分析报告**（含 5 张 SVG 图表、本期点评、自证校验；`--pdf` 可导出 PDF） |
| `scripts/journal.py` | **交易 / 预测台账**（append-only；数据落在 `_journal/`，已被 .gitignore 屏蔽，个人数据不进仓库） |
| `scripts/calibrate.py` | **校准报告**：业绩归因 / 止损执行率与漏单 / 预测校准与 Brier / 参数建议 |
| `scripts/risk.py` | **账户级风控闸门**：回撤熔断 · 行业敞口 · 市值档 · 风险预算 |
| `scripts/fees.py` | 费用敏感度：最小可做资金线 · 成本矩阵 · 临界涨幅 |
| `scripts/pit.py` | **PIT 时点回测** + 前视偏差量化实验 |
| `scripts/verify.py` | 多接口交叉校验（快照 / K 线三源 / 板块资金两口径） |
| `scripts/health.py` | 数据源健康自检 + 当日结论置信度 |
| `scripts/selfcheck.py` | **自证校验**：不依赖第二数据源的**内部恒等关系**断言（主力=大单+超大单、量额单位、涨停价、费用方向…），已**常驻**接入 `report.py` |
| `scripts/smoketest.py` | **随机票全链路冒烟 + 已知错误模式扫描**（改完代码先跑它） |
| `scripts/check_privacy.py` | **提交前隐私闸门**（纯标准库）：四种模式 —— 暂存区 / 提交信息 / 全量 / 历史审计；**禁词清单放在仓库外**，避免"禁词本身被提交" |
| `scripts/install_hooks.py` | **一条命令重装隐私闸门钩子 + 端到端自检**（`.git/hooks` 不进版本库，换机器 / 重新 clone 后必须重装） |
| `scripts/md2html.py` | 把 `.md` 渲染成**自包含单文件 HTML**（可预览 / 可打印，代码块带「复制」按钮，无外链依赖） |
| `scripts/factors.py` | **量化因子挖掘**：33 个因子的横截面 IC / ICIR / t 值 / 胜率 / 分组单调性（财务因子强制走 PIT） |
| `scripts/charts.py` | **纯 SVG 图表**（零依赖 / 无 JS / 可导出 PDF）：K线+均线+成交量、资金流柱状、板块相对强度对比、**回测逐笔高低范围**、收益分布直方图；深色仪表盘风格，涨红跌绿 |
| `examples/report_template.md` | 五段式**文本**输出模板（用于对话内输出） |
| `examples/notes_sample.json` | `report.py --notes` 的人工研判注入样例 |

### 本仓库自身的隐私闸门（给贡献者）

这是个**公开**仓库，所以 `.githooks/` 里放了两个提交钩子 —— 提交前自动扫一遍，命中就阻断：

```bash
python scripts/install_hooks.py          # 安装 + 自检（新机器 / 新 clone 后跑一次）
python scripts/install_hooks.py --check  # 只体检，不改动
```

| 钩子 | 管什么 |
|---|---|
| `pre-commit` | **文件内容** |
| `commit-msg` | **提交信息** —— `pre-commit` 根本看不到它，而它同样是公开的 |

三条设计约束：
1. **禁词清单放仓库外**（`_privacy_terms.txt`，已 gitignore）—— 把"要禁的字符串"写进一个会被提交的文件，等于把禁词又公开一次；
2. **找不到 python 解释器时按失败处理**，不静默跳过 —— "看起来装了、其实没在工作"的闸门比没有更危险；
3. **装完自检两个方向**：含禁词的信息要被拦（能拦）、正常信息要放行（能放）。只测一边等于没测。

---

## 二、环境要求

- **Python 3.8+**，**仅用标准库**（`urllib` / `json` / `ssl` / `re` / `math` / `time`），**无需 pip 安装任何包**。
- 需要能访问：`qt.gtimg.cn`、`push2*.eastmoney.com`、`money.finance.sina.com.cn`、`web.ifzq.gtimg.cn`

---

## 三、如何加载到不同软件

### 1. WorkBuddy / Claude Code / Claude.ai（标准 Agent Skills）

**第一步 · 拿到文件**（二选一）：

```bash
# 方式 A：git clone
git clone https://github.com/Y5H1ggs/niuma-stock-screener.git
```

方式 B：仓库页 → 绿色 `Code` 按钮 → `Download ZIP`。

**第二步 · 把整个 `选股分析牛马工具/` 文件夹**放到 skills 目录：

```bash
# 个人级（本机所有项目可用）
# macOS / Linux
~/.claude/skills/选股分析牛马工具/
# Windows
%USERPROFILE%\.claude\skills\选股分析牛马工具\

# WorkBuddy
~/.workbuddy/skills/选股分析牛马工具/
```

放好后，Agent 会读取 `SKILL.md` 的 frontmatter（`name` + `description`）自动识别何时调用。

### 2. Cursor

Cursor 用 `.cursor/rules/*.mdc`。做法：在项目里新建 `.cursor/rules/a-share-t1.mdc`，
把 `SKILL.md` 正文粘进去，frontmatter 改为：
```
---
description: A股 T+1 隔日接力选股
globs:
alwaysApply: true
---
```
脚本仍可 `python scripts/xxx.py` 直接跑。

### 3. 任意其他 Agent / 本地 LLM 工作流（通用做法）

该 Skill 就是**纯文本 + 纯标准库脚本**，任何能读文件、执行命令的环境都能用：

- 把 `SKILL.md` 作为 system prompt 的一部分（或作为首条上下文）注入；
- 需要数据时按 `references/datasources.md` 调接口，或直接 `python scripts/ds.py`；
- 需要选股时依次跑 `scan_pool.py` → `sector_compare.py` → `mood.py` → `backtest.py`。

### 4. 纯手工 / 不加载任何软件

`references/` 下的四份文档本身就是一份**可独立阅读的 A 股短线研究手册**——
接口、规则、坑点、回测方法都写全了，照着跑脚本即可。

---

## 四、快速上手

```bash
cd scripts

# 0) 【首次必做】基础数据录入：把 config.example.json 复制为 config.json，填上
#    你的资金 / 交易权限 / 费用率 / 止损纪律。填完先自检：
python cfg.py

# 0.5) 自检：确认数据源连通
python ds.py

# 1) 情绪温度（今天该不该做接力）
python mood.py

# 2) 候选池扫描（三选一）
python scan_pool.py strong      # 强势口径：涨停/准涨停
python scan_pool.py trend       # 趋势口径：排除涨停，走势稳定向上
python scan_pool.py potential   # 潜力口径：未涨停+量能+资金

# 3) 板块横向对比（铁律 1，必做）
python sector_compare.py 600000

# 4) 形态回测（该股历史同类形态次日胜率）
python backtest.py 600000

# 5) 一键出 HTML 深度分析报告（同款版式）
python report.py 600000 --title 深度分析
python report.py 600000 --title 午盘深度分析 --notes notes.json --sector-top 4
python report.py 600000 --pdf                       # 额外导出 PDF（复用本机 Chrome）
python report.py 600000 --anon                      # 脱敏版：不含账户资金与持仓口径（发给别人时用）

# 6) 次日涨跌概率（形态邻域回测；开 / 收 / 盘中三口径**分开报**）
python predict.py 600000 --cost 10.00   # --cost 传建仓成本（此处为示例值）

# 7) 交易前风控审查（账户级）
python risk.py status            # 当前回撤 / 熔断状态 / 风险预算
python risk.py check 600000      # 这笔允许开吗、建议多少股

# 8) 台账与校准（「后续都需要校准」的执行体）
python journal.py add   --code 600000 --name 示例标的 --price 10.00 --shares 500
python journal.py close --id 20260105-600000 --price 10.20 --reason take_profit
python calibrate.py all          # 业绩归因 / 止损执行率 / 预测校准 / 参数建议

# 9) 诊断类（怀疑数据或结论时先跑这些）
python health.py --quick         # 数据源可用率 + **今日结论置信度**
python verify.py 600000          # 多接口交叉校验（双源比对）
python fees.py                   # 费用敏感度与最小可做资金线
python pit.py 600000             # 该股的公告日历（PIT 视角：那天能看到什么）
python patterns.py 600000        # 扩展形态库（22 条）回测
python patterns.py 600000 --rule "close>ma5 and rsi14<40 and pctb<30"   # 自定义形态
```

**关于 `report.py`**：数值、板块相对强度、超买超卖判定、情景概率全部**自动计算**，
不传 `notes.json` 也能出一份完整报告；传入 `notes.json` 可用人工叙述覆盖
`verdict / bull / bear / scenarios / watch / oneline` 几段定性内容
（样例见 `examples/notes_sample.json`）。

> 所有脚本的**资金规模 / 板块权限 / 价格上限**都来自 `config.json`，脚本里没有任何预设值。
> `report.py` 会调用 `backtest.py` 作为模块，两者须放在同一目录。

---

## 五、内置硬约束（可在 SKILL.md 顶部按自己情况修改）

1. **只做你权限内的板块**：由 `config.json` 的 `allowed_prefixes` 决定（默认沪深主板 60/000/001/002/003）
2. **仓位模式**：默认全仓单吊，100 股整手
3. **3 板及以上默认不接力**
4. **板块内资金接力偏好必查**
5. **禁止绝对化表述**：所有情景必须带概率，≥50% 标高概率
6. **不给买卖指令**：只做客观推演，末尾强制风险提示
7. **账户级风控闸门**：回撤 ≥ `max_drawdown_pct` 自动暂停开仓 · 禁止连续押同一行业（`max_same_sector_streak`）· 单笔潜在亏损 ≤ 权益 × `risk_per_trade_pct`
8. **样本 <30 笔不许调参**：证据强度不够时**拒绝修改任何参数**——见 `references/calibration.md`

---

## 六、路线图：后续更新计划

> **✅ 当前状态（v1.2.8 / 2026-09-22）：P0 · P1 · P2 共 9 项已全部实现，并在后续修订中持续修复实测缺陷。** 详见 `CHANGELOG.md`。
>
> | 新增模块 | 对应路线图项 |
> |---|---|
> | `pit.py` + `ds.financials/fundamentals_at` | P0-1 PIT 时点回测（用**公告日**闸门消除前视偏差） |
> | `risk.py` | P0-2 账户级风控（回撤熔断 / 行业敞口 / 市值档 / 风险预算） |
> | `journal.py` + `calibrate.py` | P1-3 止损执行率与预测校准（含 Brier 分数与业绩归因） |
> | `fees.py` | P1-4 费用敏感度（最小可做资金线） |
> | `verify.py` | P1-5 多接口交叉校验 |
> | `health.py` | P2-6 数据源健康自检 |
> | `report.py --pdf` | P2-7 PDF 导出 |
> | `patterns.py` | P2-8 形态库扩展（22 条）+ 自定义形态 DSL |
> | `cfg.MARKETS` | P2-9 多市场适配（硬约束配置化） |
>
> **自升级机制**：`references/calibration.md`（参数调优裁判规则）+ `CHANGELOG.md`（版本沿革）
> + SKILL.md 的「自升级与校准协议」。核心原则：**样本 <30 笔不许调参；改动必须留痕**。
>
> 下面保留原始规划文本，作为设计意图的存档。
>
> **关于 Gap**：本项目的路线图围绕一个核心指标推进——**Gap = "纸面可得的收益" 与 "纪律可执行后的真实收益" 之间的落差**。
> 已有研究表明，短线策略的收益瓶颈往往不在选股信号本身，而在**风控执行的完整度**与**回测口径的干净程度**。
> 下面每一项都是在做同一件事：**把 Gap 压小**。

### P0 · 核心（优先做）✅ 已实现

| 项目 | 目标 | 为什么重要 |
|---|---|---|
| **1. PIT 时点回测（Point-in-Time）** | 引入历史时点快照数据，消除财报/财务指标的前视偏差（look-ahead bias）；把回测口径从"纯价格量能形态"扩展到**可以安全使用财务因子** | 没有 PIT，任何带财务因子的回测都会高估收益——**前视偏差是 Gap 的第一大来源**。做完这一项，才能把基本面维度纳入评分而不自欺 |
| **2. 风控模块：行业中性 / 市值中性 / 最大回撤限制** | 从**单笔纪律**升级到**账户级风控**：按行业与市值分组约束敞口（避免全仓压在一个赛道），并加入最大回撤熔断 | 全仓单吊模式下，单一行业的系统性风险无法分散；**账户级回撤限制**是把"个人纪律"变成"机械约束"，这才是能真正缩小 Gap 的一环 |

### P1 · 验证（让数字可信）✅ 已实现

| 项目 | 目标 |
|---|---|
| **3. 止损执行率追踪** | 记录"该止损未止损"的漏单，统计真实执行率，把"理论可回收的收益"折算成"实际回收的收益" |
| **4. 费用敏感度分析** | 把双边费用与**最低佣金摩擦**纳入回测，输出"多小的单不值得做"的临界资金线 |
| **5. 多接口交叉校验** | 同一指标双源取数自动比对，冲突时在报告上标红，替代目前"单源 + 降级链"的做法 |

### P2 · 体验（让工具好用）✅ 已实现

| 项目 | 目标 |
|---|---|
| **6. 数据源健康自检** | 记录各接口当日可用率，输出降级链命中日志，方便判断当天结论的置信度 |
| **7. 报告导出 PDF / 长图** | 复用 headless Chrome 一条命令出 PDF，便于存档与分享 |
| **8. 形态库扩展** | 从 12 种内置形态扩充，并支持自定义形态描述（DSL） |
| **9. 多市场适配** | 把硬约束做成可配置项，支持其他市场/板块口径（需自行确认交易权限） |

> ⚠️ 以上 9 项**已全部实现**，实施记录见 `CHANGELOG.md`。
> 后续演进将围绕「**用实盘校准驱动**」推进：等交易台账积累到 **30 笔以上**，才谈参数调优；
> 在那之前，重点是继续记录、继续验证口径——**不为了"有更新"而改参数**。
> 如果你有更需要的功能，欢迎提 Issue 讨论。

---

### 下一步 · v1.3 计划（**尚未实现**）

> **排序原则：先补可信度，再补功能。**
> 数据口径不可信时，增加指标只是在放大误差——本项目 57 条踩坑记录里，约三分之一源自
> "**看起来完全正常的错误数据**"（占位符被读成 0、单位差 100 倍、比较错了字段）。
>
> **纪律前提**：凡涉及**调参**的项目，一律等台账积累到 **30 笔已平仓交易**
> （见 `references/calibration.md`）。在那之前，只有"补校验"与"补记录"两类可以动。

| 优先级 | 方向 | 项目 | 为什么排这个位置 |
|---|---|---|---|
| **P0** | 数据可信 | ~~**1. 自证校验常驻化**（`selfcheck.py`）~~ ✅ **v1.2.5 已实现** | 已落地：**17 项内部恒等关系断言**（主力=大单+超大单、量额单位、涨停价、费用方向、K线对齐…），**常驻接入 `report.py`**，失败项进报告「数据完整度」卡；并配 `smoketest.py` 把"偶然发现"变成"每次自动跑"。校验器自身也做了注入测试（8 类错误全部命中、零误报） |
| **P0** | 数据可信 | **2. PIT 实验扩样重跑** | 当前 30 只票 × 60 信号日得出的偏差**与理论方向相反**，样本不足以定性。需 `--days 500` + 多因子（ROE / 营收增速 / 毛利率）重跑，否则 PIT 只是"写了个函数"而已 |
| **P0** | 闭环 | **3. 预测台账自动化** | `predict.py` 输出自动落 `forecasts.json`、自动结算。**不落台账的概率预测等于没做**——这一步目前是手动的，随时会被漏掉 |
| **P1** | 口径 | **4. 封板质量入评分** | 首封时刻 + 打开分钟数（数据源已通）目前只在人工判读里用，未进机械评分。**硬板与尾盘炸板的差别应该体现在分数上**，而不是靠叙述 |
| **P1** | 口径 | **5. 情绪周期状态机** | `mood.py` 现在只输出三个指标，"冰点 / 启动 / 发酵 / 退潮"四态靠人工判读。做成状态机后可让仓位建议随状态自动降档（冰点期直接禁止接力） |
| **P1** | 口径 | **6. 主线迁移跟踪** | 连续多日的板块资金流向图，自动识别"主线换挡"（如 09-21 医药 → 09-22 电子）。目前靠人工对比两天的榜单 |
| **P2** | 体验 | **7. 时点提醒自动化** | 14:00 尾盘扫描 / 09:26 竞价校准做成定时任务，到点自动出报告（当前需手动触发） |
| **P2** | 体验 | **8. 报告图表化** | HTML 报告加 K 线图、资金流向曲线、板块相对强度条形图。目前全是表格 |
| **P2** | 体验 | **9. 形态 DSL 扩展** | 支持多条件组合与自定义指标，并把常用组合持久化为命名形态 |
| **—** | 纪律 | **10. 积累 30 笔样本**（不是功能） | **这是当前唯一真正重要的事。** 在此之前，上面 9 项里凡"调参"性质的都不该动 |

> ⚠️ 以上为**计划，不是承诺**；优先级会随实盘反馈调整。
>
> **明确不做的事**（写出来避免预期错位）：
> ① 不引入需要付费数据源的深度行情（Level-2 / 逐笔委托）；
> ② 不做自动下单或账户对接；
> ③ 不做"荐股"式标的推荐——本工具只给**候选池 + 概率 + 情景树**。

---

## 七、许可与免责声明

**License：MIT**（全文见 `LICENSE`）—— 代码可自由使用、修改、分发，**包括商业用途**。

> **为什么授权与声明分开写**：GitHub 的协议识别器要求 `LICENSE` 文件必须是**标准全文**，
> 任何附加段落都会导致识别失败（表现为仓库页显示 `NOASSERTION`）。
> 所以本项目的做法是——**授权用纯净的标准 MIT，声明写在 README 里**。

**免责声明**（独立声明，不改变上面 MIT 对代码的使用许可）：

本项目仅供个人研究与学习使用。所有数据来自第三方公开接口，可能存在延迟、缺失或错误；
所有统计与推演**不构成任何投资建议**。据此操作，风险自担。

> The trading strategies, indicators, ratings and backtest results in this project are for
> personal research and educational purposes only and do NOT constitute investment advice.
> All market data is retrieved from public third-party APIs and may be delayed, incomplete
> or inaccurate. Use at your own risk.
