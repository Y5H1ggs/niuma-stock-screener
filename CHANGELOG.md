# 更新日志（CHANGELOG）

> 本项目遵循「**校准驱动的版本演进**」：每一次升级都由**实盘校准结论**或**新验证的口径**触发，
> 不做无依据的功能堆砌。
>
> 版本号规则：`主版本` = 口径或架构变更 · `次版本` = 新模块 · `修订` = 缺陷修复。
>
> **每次升级必须同步三样**：代码 → 本文件 → `~/.workbuddy/skills/选股分析牛马工具/`（安装目录）。

---

## v1.2.11 — 2026-09-23 · 尾盘实测修缺陷：`mood.py` 崩溃 + `.gitignore` 按产物类型补全

### 修复

| 项 | 现象 | 修法 |
|---|---|---|
| `mood.py` 在板块资金取数失败时**整体崩溃**（P67） | `TypeError: unsupported format string passed to NoneType.__format__`，栈指向 `f'主力 {zl:+.2f}亿'`。情绪温度**一个字都没输出** | 只在 `chg is None` 分支做了兜底，漏了 `zl` 本身。改为 `None` 先转文本并显式打印「**取数失败**」，`chg is None or zl is None` 一并兜底 |
| `.gitignore` 只按扩展名挡产物（P59 类） | 扫描中间产物 `_cache_*.json` / `stage*.json` 全部显示为未跟踪，`git add -A` 会一并提交 | 按**产物类型**补规则：`_cache_*.json` / `stage*.json` / `stage_*.json` / `_universe*.json` |

### 为什么这两条值得单独发一版

崩溃发生在**尾盘只剩 8 分钟的调用场景**里。「情绪温度」是准入清单第 6 项
（非冰点判定）的**唯一数据来源** —— 崩了就等于这一维完全没有数据，
任何"情绪活跃 / 冰点"的判读都只能是编的。而 `.gitignore` 那条是
**上一轮隐私事故的同类形态**：只按扩展名挡，就会漏掉"扩展名相同但用途不同"的文件。

### 新增踩坑

- **P67** 取数失败的 `None` 直接进格式化字符串 → 整个脚本崩溃。
  规则：**`None` 不能进 `%`/f-string 数值位**；失败要打印成「取数失败」而不是跳过
  （跳过 = 静默丢一维数据；标注出来，使用者才知道这一维今天不能用）。
- **P68** 全量抓取在"接口间歇空返回"下**耗时不可控** → 直接吃掉整个尾盘窗口。
  实测：抓全市场 36 页跑了 **8 分 39 秒仍未产出**，被迫 kill，交付时点已过 14:45。
  修法不是放弃严谨，而是**用口径推导最小完备集合**：强势口径（涨幅 6~9.5%）
  必然落在涨幅榜最前面，只抓前 10 页（1000 只）就已完备，失败页照样逐页补抓、
  照样输出涨幅分布自检。耗时 **>519s（未完成）→ 114s**，池子完整
  （最高涨幅 +10.16%、涨停 21 只、f62 取数失败 0 只）。
  规则：**范围可以缩，严谨不能缩。**

---

## v1.2.10 — 2026-09-22 · 隐私闸门可移植化（`.githooks` + 一条命令重装）

> **为什么是 v1.2.10，不是 v1.3.0**：README 路线图里的「**下一步 · v1.3 计划**」是
> **已经对外承诺**的一组功能（自证校验常驻化 / PIT 扩样重跑 / 台账自动化 / 封板质量入评分 /
> 情绪周期状态机…）。**版本名一旦被预留，就不能被中间的工程改动占用** ——
> 否则"v1.3 已发布"与"v1.3 尚未实现"会同时出现在同一个 README 里，读者无法判断。
> 本次属工程与文档层面的修补，故走 v1.2.x 修订位。
>
> （曾一度打成 v1.3.0 并推送，已回退：删除该 tag，改发 v1.2.10。）

### 为什么改
`.git/hooks/` **不进版本库** —— 换机器、重新 clone、换工作副本之后，闸门会**无声消失**，
而提交照常成功。这正是"闸门看起来装了、其实没在工作"的形态：上一轮隐私处置里，
提交信息被漏检的那一次，就**没有任何机制能发现闸门缺位**。

### 新增
- `.githooks/pre-commit` · `.githooks/commit-msg`：钩子**模板**，受版本控制、可被 review。
  模板里**不含任何本机路径**（否则等于把用户名写进公开仓库）。
- `scripts/install_hooks.py`：把模板装进 `.git/hooks/`，并在 shebang 后**注入本机解释器路径**
  —— 本机路径只落在 `.git/hooks/`（不进版本库）。`--check` 只体检、不改动。
- **fail-closed**：找不到 python 解释器时**按失败处理并阻断提交**，不做静默跳过。

### 自检（装完立刻跑，三个方向）
1. `commit-msg` + 含禁词的提交信息 → 期望 **exit 1**（能拦）
2. `commit-msg` + 正常提交信息 → 期望 **exit 0**（能放）
3. `pre-commit` 扫当前暂存区 → 期望 exit 0

只测"能放"等于没测；只测"能拦"会把正常提交堵死 —— **两个方向都要测**。
另加 `core.hooksPath` 检测：该配置一旦被设置，Git 会**忽略** `.git/hooks`，前两个钩子形同虚设。

### 实测
`python scripts/install_hooks.py` → 3 项自检全部通过；`--check` 复跑结果一致；
模板与本机路径均无泄漏；PATH 清空且未设 `PRIVACY_PYTHON` 时 → **exit 1** 并给出三种解法。

### 配套踩坑（P63~P66）
- **P63** 钩子只装在 `.git/hooks/` → 换机器后**无声消失**（已由本次可移植化解决）
- **P64** 钩子脚本被转成 CRLF → `sh` 报 `\r: command not found`，闸门**静默失效**
- **P65** `grep -c $'\r'` 数 CR **假阳性**（33 行报 33 个 CR，字节级复核实为 0）
- **P66** `python -c "…"` 里的反斜杠被 shell 吞掉 → **正则静默失效**（`\d` 变 `/d`）

P65 与 P66 是同一天踩的同一个坑的两种形态：**shell 引号会静默改写你传给程序的字符串。**
凡带转义 / 正则 / 控制字符的检查，都要写成文件执行，或先用 `repr()` 验证模式本身。

---

## v1.2.9 — 2026-09-22 · 清除仓库中的个人实盘数据 + `journal.py` CLI 缺陷修复

### 安全：公开仓库不再包含任何账户口径

全仓库扫描出 **15 处**账户数据（真实标的、买入价、股数、平仓价、盈亏金额、本金、可用资金），
分布在 7 个文件，已全部替换为**示例值或百分比**：

| 文件 | 处理方式 |
|---|---|
| `README.md` | 台账示例整条换成「示例标的 + 整数价格」的通用样例；`--cost` 示例改为示例值并注明 |
| `SKILL.md` §0 | 费用实证删去本金与金额，改为「一笔万元以下的 T+1 交易，双边约 0.21%，其中 10 元来自最低佣金」 |
| `SKILL.md` §2.5.2 | 处置时点表由「真实价格 + 亏损金额」改为「相对成本的百分比」，**结论「延迟 35 分钟多亏 2.6pp」不变** |
| `references/pitfalls.md` | 3 处案例去标识、金额改百分比 |
| `config.example.json` · `fees.py` · `journal.py` · `predict.py` | 示例参数改为通用值 |

**保留**：公开股票代码在 API 与命令示例中的用法 —— 那是通用样例，不含账户信息。

> 本条目刻意**不引用被替换的具体数值**。记录「已移除敏感数据」时把原值写回来，
> 等于把泄露换个位置再放一遍 —— 这与代码里写 `# 密码是 xxx` 再删掉注释是同一类错误。

### 修复：`journal.py` CLI 两个缺陷

| 缺陷 | 现象 | 修法 |
|---|---|---|
| `close` 找不到记录时**报成成功** | 打印「已平仓： 找不到该笔」 | 明确报错 + 非零退出 + 提示 id 格式（建仓日+代码） |
| 漏参 / 参数类型错时抛 Python 堆栈 | `float(None)`、`could not convert string to float` | 新增 `_req()` 统一给出「缺少必填参数 --price」这类提示 + 退出码 1 |

另：`add` 命中同日同代码时（id 自动加 `b` 后缀）会主动提示"若是重复执行，请核对后删除"。
实测 4 条错误路径均给出明确提示且 `exit=1`。

### ⚠️ 仍未处理：提交历史里仍有旧数据

本次只清理了 HEAD。旧数据由若干历史提交引入并已推送到公开仓库；
**commit message 里也留有相同内容**。要彻底抹除需重写历史，
但那会改变所有后续提交的 SHA、需要重打 tag 并强推 —— **待确认后再做**。

---

## v1.2.8 — 2026-09-22 · 抬头卡补全 + 打印样式 + 三处渲染缺陷修复

### 抬头卡：从"右上角留白"到"价格 + 4 项关键指标"

**根因不是留白，是换行位置**（P59）：`.mast` 是 `flex-wrap:wrap`，左栏 div 没有 flex 约束，
其 max-content 宽度＝tagline 排成一行的长度 → 占满整行 → 右侧价格块被挤到第二行，
而它自身只有 90px 宽，于是呈现为"右上角一大片空"。

**修法**：① 左栏 `flex:1 1 52%; min-width:0`（`min-width:0` 是关键）；
② 右栏重构为 `.mastr`，承载「大字价格 + 2×2 关键指标格」——把空白**用起来**而不只是对齐：

| 指标 | 含义 |
|---|---|
| 模型评级 | 评级文字 + 总分（按档位着色） |
| 当日主力 | 主力净额（红涨绿跌着色）；取不到印「不可用」 |
| 相对强度 | **双基准中更不利者**的 pp 值（与板块卡同源） |
| 250 日位置 | 一年分位；次新股无 250 日数据时印「不可用」 |

### 其余三处修复

| 问题 | 根因 | 修法 |
|---|---|---|
| 标题徽章被拆成「…深度分 / 析」，后半段跑出圆角底 | 给左栏限宽后 `h1` 内的徽章可折行 | `h1` 改 flex 容器；徽章 `nowrap + min-width:0 + ellipsis` |
| 点评行「资金面 73%」多出一块**浅金底** | 新写的 `.hi`/`.lo` **撞上报告已有的全局工具类** `.hi{background:#FBF3DE}`（P60） | 改名 `hvup`/`hvlo` 加命名空间 |
| 预测的「邻域案例」列表**日期与指标不同源** | `neighbourhood()` 元组第 5 位存了**次日**日期 `nx['d']`，却与**信号日**特征并排打印，产出「2026-09-22 涨+10.00%」这种自相矛盾的行（P62） | 改存信号日日期 `k[i]['d']`，表头改写为「信号日 → 次日实际表现」。**概率计算本身无误**（`stats()` 不读该位） |
| PDF 打印无任何显式样式 | 原 CSS 完全没有 `@media print` | 加 `print-color-adjust:exact`、`@page A4`、`.card/.sub/svg{break-inside:avoid}` |

### 新增验收方法：**渲染成图片再看**（P61）

今天 4 个视觉问题**全部通过了**文本提取、正则计数、div 配对、`<svg>` 计数等结构级检查。
新增可复用流程：
1. `report.to_pdf()` 打 PDF（注意绝对路径，P46）；
2. `pymupdf` 渲染指定页/裁剪区为 PNG：`get_pixmap(dpi=300, clip=Rect(...))`；
3. 看图核对。必要时额外做**像素亮度抽样**，用于判定"深底背景有没有被打印丢弃"
   （该故障表现为白底白字且文件大小正常，极难察觉）。

**顺带把报告 PDF 能力补齐验证**：本次实测 19 页 / 2.7 MB，首页深色像素占比 47.6%
→ 深底背景确认进入 PDF。

---

## v1.2.7 — 2026-09-22 · 报告脱敏模式（`--anon`）+ `ulist` 入参修复

### 新增 `report.py --anon`（脱敏模式）

| 位置 | 默认（本人看） | `--anon`（给别人看） |
|---|---|---|
| 数据速览 · 资金占用行 | `X 股 = Y 元（config 资金 Z）` | `—（本报告为脱敏版，不含账户资金与持仓口径）` |
| 风险提示 · 成本条 | `按 config 资金 Z 元，双边费用约 N 元` | 只讲成本率（0.18%），不含金额 |
| 风险提示 · 纪律条 | `你的止损纪律为 −4.0%，兑现目标 2.0%` | 改为通用纪律表述（"止损位须在买入前算好并机械执行"） |
| 板块权限警告 | 打印 `allowed_prefixes` 列表 | 不输出（那是账户持有人自己的权限，与标的无关） |
| 终端日志 | `（资金 Z 元，来源：config.json）` | `（脱敏模式：不读取也不展示账户资金）` |
| 自证校验股数 | 由 `cash ÷ 价格` 推定 | 固定 100 股（费用模型自洽断言与持仓规模无关） |

实现要点：`gather(..., anon=True)` 内**把 `cash` 归零**，下游所有派生值自动退化为中性；
渲染层按 `d['anon']` 分支。**归零而非"传个假数"** —— 假数会在报告里留下一个不存在的事实。

**验收方式（不靠肉眼）**：生成后扫描 HTML，断言不含 `config.json` / `allowed_prefixes` /
`你的` / 本金数值；再正则取出「资金占用」行确认已渲染为脱敏文案。
实测 6 项检查全部为「无」，「资金占用」行确认为脱敏版。

### P58 `ds.ulist()` 只接受列表，传字符串静默返回 0

- **现象**：`ds.ulist('600000')` 让报告显示"主力净额 0 万"，看起来像资金持平，实为取数失败。
- **根因**：`','.join(secid(c) for c in codes)` 对字符串**逐字符**迭代，拼出无意义 secid，
  接口返回空 `diff` → 返回 `{}` → 调用方 `.get('f62') or 0` 得到 **0**。全程不抛异常。
- **修法**：入口加 `if isinstance(codes, str): codes = [codes]`。
  这是"让入参更宽容"，**不是改返回类型**（P55 的教训：那是破坏性变更）。
- **通用规则**：**入参要宽容，返回值要严格**；任何"恰好是 0／空"的关键指标，
  先怀疑取数失败（过 `_ok()`），再当成"真的是 0"参与打分。

---

## v1.2.6 — 2026-09-22 · 图表可读性修复 + 接口契约与文档一致性

### 视觉修复

| 问题 | 根因 | 修法 |
|---|---|---|
| K 线底部日期刻度**压在成交量柱上** | 布局没给日期留带 —— 柱体基线在 `h-16`，日期文字写在 `h-13`，字形顶边只比基线低 3px | 新增 `date_h = 30` 专用带，日期移到 `h-12`，并在柱底与文字之间补 3 条刻度短线 |
| hero 卡自证行**贴左边缘、深底上看不清** | 该行用的是浅底配色 `.tiny`（`--muted`），且没有横向留白（其余元素都有 24px） | 新增 `.hero .hchk`：右对齐 + 深底专用浅色 + `0/24px` 留白，通过数用绿色高亮 |

**新增「本期点评」区**（hero 卡内，四维得分下方）：用**四维达成率**（得分 ÷ 满分）机械生成，
只做一件事 —— 把**最低的那一维**指出来（最高/最低分别用金/红着色）。

> 为什么不做成主观研判长句：那会和「评级理由卡」重复，而且容易滑向抒情。
> 点评刻意保持"机械、可复算"，并显式声明不构成买卖指令。

### 接口契约与文档一致性

| 项目 | 问题 | 处理 |
|---|---|---|
| `ds.kline()` | 返回**二元组** `(数组, 源名)`，但 docstring 完全没写 → 直接喂给画图函数**不报错、只画出空白图** | ① docstring 显式写明返回类型与两种误用症状；② 加语义别名 `kline_src()`；③ 加 `kline_list()`（只要数组就用它） |
| `references/pitfalls.md` | **P50~P53 只写在 CHANGELOG 里，从未落进踩坑册** —— 文档内部不一致 | 补齐 P50~P53，并新增本轮 P54~P57 |

⚠️ **本轮走过一次弯路，如实记录**：我一度把 `ds.kline()` 改成"直接返回数组"来消除这个坑，
结果打断了 **7 处** `k, src = ds.kline(...)` 调用方。
**契约瑕疵的正确修法是"补文档 + 加显式新接口"，不是悄悄改返回类型** ——
后者把"可能误用"升级成了"一定崩"。

### 本轮新增踩坑（P54~P57）

| 编号 | 一句话 |
|---|---|
| **P54** | 画布上任何"贴着图形放"的文字都必须有**独占留白带**；用 `h-13` 这类从画布边缘倒推的写法最容易出问题 |
| **P55** | 公共函数返回类型必须写进 docstring；凡"用错也不报错"的接口，都要额外提供一个不可能用错的入口 |
| **P56** | `rg` **不支持 lookbehind**，带 look-around 的模式会**静默返回"无匹配"**。据此得出"没有调用方"的否定性结论，差点把破坏性回归推上线 |
| **P57** | 同一文件的**两处编辑并发提交会互相覆盖**（工具都报 success）。必须串行修改，且用实测数据复核，不能只看回执 |

**自证方式**（P54，不靠眼睛）：用正则取出柱体 `y + height`（真实底边）与日期 `<text>` 的
`y − 字号`（字形顶边），断言间距 > 3px。实测 5 只票（n = 20/30/40/55/60）全部 **+22.5px**。

---

## v1.2.5 — 2026-09-22 · 自证校验常驻化 + 量化因子挖掘 + 自检体系

### 新增模块

| 模块 | 作用 |
|---|---|
| `scripts/selfcheck.py` | **自证校验（常驻）**：不依赖第二数据源的**内部恒等关系**断言，共 **17 项** —— 主力=大单+超大单、主力+中单+小单=0、量额单位自洽（隐含均价必须落在当日 `[low, high]`）、涨停价 = prev×(1+limit%)、涨跌幅 = 手算、内外盘 = 成交量、K 线末根日期对齐、费用方向与量级。**已接入 `report.py`**：每次出报告自动跑，失败项写进「数据完整度」卡 |
| `scripts/smoketest.py` | **随机票全链路冒烟 + 已知错误模式扫描**：随机抽 N 只主板股跑全套模块；同时对源码扫描已踩过的错误模式（P44/P45/P49 类）。支持 `--codes` 精确复现失败 |
| `scripts/factors.py` | **量化因子挖掘**：33 个因子 × 横截面 IC / ICIR / t 值 / 胜率 / 分组单调性。财务因子**强制走 PIT**（公告日闸门） |
| `scripts/charts.py` | **纯 SVG 图表（零依赖 / 无 JS / 离线可用 / 可导出 PDF）**：K 线+均线+成交量、资金流柱状、板块相对强度对比、**回测逐笔高低范围**、收益分布直方图。深色仪表盘风格，遵守涨红跌绿 |

### 报告图表化（v1.2.5）

报告从"纯表格"升级为"表格 + 图表"，共 **5 张图**：

| 图表 | 位置 | 作用 |
|---|---|---|
| **K 线走势**（近 55 日 + MA5/10/20 + 成交量） | 四、技术分析（卡首） | 一眼看清趋势结构、均线位置、量价配合 |
| **资金流向**（近 14 日主力净额柱状） | 独立卡（多空研判前） | 把"连续净流出"从一句话变成可见的形态 |
| **相对强度对比**（个股 vs 各所属板块中位） | 二、板块横向对比 | 视觉上突出**最下面那条**——因为主判据取「更不利者」，不是最好看的那个 |
| **回测逐笔高低范围** | 五、历史回测 | ⭐ **本次的重点**：见下 |
| **次日收益分布直方图** | 五、历史回测 | 看分布是"聚成一堆"还是"两头分离" |

**为什么回测要画"逐笔范围"而不是只给均值**：
均值 +0.5% 可能是"全部挤在 0 附近"，也可能是"一半 +5%、一半 −4%"——
**对 T+1 隔日模式，后者才是真实风险敞口**。只报均值会把高波动美化成"稳健小赚"。
逐笔画出高低范围与开→收，离散度就无处可藏。

**两条可读性硬约束**（实测踩坑后固化，勿回退）：
1. **viewBox 宽度必须匹配容器**：报告 `.wrap` 是 `max-width:960px`，减去内边距后图表可显示宽度约 **900px**。
   写成 860 会被拉伸、写成 1100 则整体缩小导致字号缩水到读不清。统一 `W = 900`。
2. **字号下限 12px**：曾用 9~10px，缩放到移动端 / PDF 后完全不可读，且标签互相压叠。

### 为什么"自证校验"必须常驻（而不是又一个手动工具）

交叉校验（`verify.py`）需要**两个数据源都可用**才能做；自证校验只需要**一个源**，
检查它内部是否自洽 —— 这才是它能"每次取数都跑"的原因。
本项目 49 条坑里多数属于"**看起来完全正常的错误**"（占位符被读成 0、单位差 100 倍、字段顺序读错），
它们不抛异常、数字自洽，只能靠恒等式抓出来。

**校验器本身也被校验过**（否则可能只是"永远通过的摆设"）：注入 8 类错误**全部命中**，正常数据**零误报**。

### 首次因子挖掘结论（30 只主板股 × 121 个交易日 × 5 日持有期）

- **唯一达到"有效"标准的是 `streak`（连板数）**：IC +0.0773、ICIR 0.310、t=3.11、胜率 71%
- **边际（|t|≥2）**：`rev_yoy` +0.0663 / `mom5` +0.0656 / `gap` −0.0528 / `devma5` −0.0569
- **其余 26 个因子为噪声**（|t|<2），包括**全部摆动指标**（RSI / CCI / WR / %B）与位置因子（pos60 / pos250）
- ⚠️ **关键发现：前 5 名因子全部"非单调"** —— 极值组（G1/G5）强、中间组弱。
  这意味着它们**不能用"因子值线性排序"来用**，必须做分档/阈值处理。
  **只报 IC 均值会得出"有 5 个可用因子"；把分组单调性摆出来，才发现真正可用的只有 1 个，且需非线性处理。**

### 自检体系（本轮建立）

改完代码 → `smoketest.py`（静态 + 随机票动态）；
出报告 → `selfcheck` 自动跑；
跨源核对 → `verify.py`；
因子结论 → `factors.py` 的分组单调性。
四者分工：**冒烟防崩溃 · 自证防失真 · 交叉防孤源 · 分组防挑樱桃。**

### 本轮自检发现并修复的设计缺陷

| 编号 | 缺陷 | 影响 | 修法 |
|---|---|---|---|
| **P50** | `selfcheck` 只记录失败项 | 全通过时显示"共 0 项断言"，**看起来像根本没检查** | 每项断言无论通过与否都留痕；CLI 显示"通过 N/M 项"——**校验器必须自证它跑过了** |
| **P51** | `predict.stats()` 缺样本充足性标记 | `N=2` 照样输出"P(收涨)=0%"，**用噪声冒充结论** | 新增 `reliable` 字段（n≥10）；`smoketest` 与 `predict` CLI 均已接入，样本不足时**不展示概率** |
| **P52** | `smoketest --seed` 无法复现 | 种子固定了随机序列，但 `clist` 的股票池随成交额变化 → 同 seed 两次抽到的票不同 | 新增 `--codes` 精确锁定标的用于复现 |
| **P53** | `risk.check` 只回 `allow=False` 不给原因 | 无法排查"为什么被拒" | 调用方打印 `reasons[0]`，实测可直读"G2 行业敞口：上一笔同属电网设备 → 拒绝" |

---

## v1.2.2 — 2026-09-22 · 两处口径修复（盘中实测发现）

| 编号 | 缺陷 | 影响 | 修法 |
|---|---|---|---|
| **P48** | 数据目录 `journal/` 与脚本 `journal.py` **同名** | 从项目根执行 `import journal` 会命中**目录**（Python 3 namespace package），报 `AttributeError: module 'journal' has no attribute 'list_trades'`。**不报 ModuleNotFoundError，而是导入一个空壳模块** —— 报错长得像"函数没写"，把排查引向错误方向 | 数据目录改名 **`_journal/`**；`.gitignore`、README、calibration.md 同步 |
| **P49** | `predict.py` 的"触及保本价"硬编码 **100 股** | 最低佣金是**固定值**，100 股摊得比 500 股贵 → 保本价算高、**概率系统性偏保守**。同一天同一形态实测：**76% vs 修正后 86%**，差 **10pp**，足以改变"给不给兑现窗口"的判断 | `stats()` 接受实际股数，CLI 增 `--shares` |

> 两处都属于"**看起来完全正常的错误**"：不崩溃、数字自洽、只是悄悄失真。
> 这恰好再次印证 v1.3 计划里「**自证校验常驻化**」的必要性——能被断言抓住的错，不该靠人眼。

---

## v1.2.1 — 2026-09-22 · 仓库元信息规范化

- **License 修正（NOASSERTION 根因）**：`LICENSE` 原本**已经是 MIT 标准正文**，
  但在末尾附加了一段自定义"仅供个人研究"声明。GitHub 的协议识别器要求 `LICENSE`
  必须是**纯净全文**，附加段落导致它直接放弃识别（仓库页显示 `NOASSERTION`）。
  → 已把附加声明移出，`LICENSE` 恢复纯净 MIT；声明并入 README「许可与免责声明」。
  **规则**：授权文件必须纯净，附加声明放文档 —— 混在一起会让**两边都失效**。
  版权行由 `选股分析牛马工具 contributors` 改为 `Y5H1ggs`。
- **README 新增 v1.3 计划**：按「**先补可信度、再补功能**」排序共 10 项
  （自证校验常驻化 / PIT 扩样重跑 / 预测台账自动化 / 封板质量入评分 / 情绪周期状态机 /
  主线迁移跟踪 / 时点提醒 / 报告图表化 / 形态 DSL 扩展 / 积累 30 笔样本）；
  并显式写出**明确不做的事**（付费深度行情 · 自动下单 · 荐股式推荐），避免预期错位。
- **GitHub Topics 已设置**（10 个：a-share / agent-skill / eastmoney / python / quant /
  stock-analysis / stock-screener / t1 / technical-analysis / trading-strategy）。
  ⚠️ Topics **不在 Settings 里**，入口在仓库首页右侧 About 区块的齿轮图标；
  本次通过 REST API `PUT /repos/{owner}/{repo}/topics` 设置（**全量替换**语义）。

---

## v1.2.0 — 2026-09-22 · 路线图 P0/P1/P2 落地 + 自升级机制

依据 `README.md` 第六节路线图实施。核心目标：压缩 **Gap = 纸面可得的收益 − 纪律可执行后的真实收益**。

### P0 · 核心

| 路线图项 | 交付物 | 关键设计 |
|---|---|---|
| **PIT 时点回测** | `scripts/pit.py`、`ds.financials()`、`ds.fundamentals_at()`、`ds.announce_calendar()` | 财报用东财 `RPT_LICO_FN_CPD` 的 **`NOTICE_DATE`（实际公告日）**做可见性闸门：某日只能看到"公告日 ≤ 该日"的最新报告期。公告日缺失时按"报告期 + 45 天"**保守回退**（宁可晚用，绝不早用）。`pit.py --demo` 用同一因子、同一股票池跑 PIT 与非 PIT 两条路径，把偏差**量化**出来 |
| **账户级风控** | `scripts/risk.py` | 四道机械闸门：G1 回撤熔断（≥15% 暂停 5 天）· G2 行业敞口（禁止连续同行业）· G3 市值档（同档降半仓）· G4 风险预算（单笔最大亏损 ≤ 权益 5%，反推仓位上限）。`cfg.py` 新增 `MARKETS` 市场档 |

### P1 · 验证（让数字可信）

| 路线图项 | 交付物 | 关键设计 |
|---|---|---|
| **止损执行率追踪** | `scripts/journal.py`、`scripts/calibrate.py` | 台账三文件（trades / forecasts / account，均不进版本库）。**用 K 线重建"本应止损"的笔数**，统计漏单与实际损失。已补录 3 笔实盘作为校准起点 |
| **费用敏感度分析** | `scripts/fees.py` | 求出**最小可做资金线**：费用占比 ≤0.3% 需 ≥5000 元、≤0.25% 需 ≥6000 元。指出"最低佣金 5 元/边 = 固定 10 元"是小资金账户的**主导成本** |
| **多接口交叉校验** | `scripts/verify.py` | 快照（腾讯 vs 东财）、K 线（东财/新浪/腾讯三源）、板块资金（board_index vs 成分求和）三组比对，冲突标红 |

### P2 · 体验

| 路线图项 | 交付物 | 关键设计 |
|---|---|---|
| **数据源健康自检** | `scripts/health.py` | 逐接口测可用性/耗时/条数，输出**当日结论置信度**：资金面与板块面同时不可用 → 按口径 v3 应"不予评级" |
| **PDF / 长图导出** | `report.py --pdf` | 复用 headless Chrome，一条命令出 PDF |
| **形态库扩展 + DSL** | `scripts/patterns.py` | 内置扩展形态库 22 条（趋势/量能/位置/涨停基因四族）+ 一行 DSL 自定义形态，如 `--rule "close>ma5 and rsi14<40 and pctb<30"` |
| **多市场适配** | `cfg.MARKETS` / `cfg.market()` / `cfg.limit_pct()` | 硬约束全部配置化：涨跌停幅度、最小交易单位、T+N、允许前缀。换市场只改 `config.json` 的 `market` 字段 |

### 自升级机制（本次新增的"元能力"）

| 交付物 | 作用 |
|---|---|
| `references/calibration.md` | **参数调优规则**：什么时候允许改参数、样本量门槛、置信区间判据、禁止行为清单 |
| `SKILL.md` §0「自升级与校准协议」 | 定义触发时点与执行顺序：实盘 → 台账 → 周校准 → 结论 → 参数/口径更新 → CHANGELOG → 同步 |
| 本文件 | 版本沿革，强制与代码同步 |

---

## 缺陷修复（本轮实测发现并修复）

| 编号 | 缺陷 | 影响 | 修法 |
|---|---|---|---|
| **P42** | 挑基准的代码用**元组下标**，把"涨幅"当"主力净额"比较（`r[2] > best[3]`，`best[3]` 实为涨幅） | 输出「题材基准 = 电网概念 +1.52 亿」，真值「数据中心 **+28.40 亿**」——差 18 倍且方向不同，**系统性偏向对标的更有利的基准** | 挑基准一律用**命名键**：`max(rich, key=lambda x: x['board_zl'])`；选出后必须**全量打印复核** |
| **P43** | `sector_members()` 的 `f6`（成交额）盘中返回占位符 | 用于绝对阈值闸门会**误杀全部票**（"该板块无一只有流动性"） | `_ok()` 先行；`f6` 仅用于相对排序，不用于绝对闸门。**第三次踩到 `_fl()` 不做 `_ok()` 校验**（P40 → P43） |
| **P44** | 模块在 **import 时重绑 `sys.stdout`** | 旧 wrapper 被回收时连带关闭底层 buffer，调用方随后 print 报 `I/O operation on closed file` | 重绑一律放进 `__main__`；已在 journal/risk/pit/predict 四个文件加注禁止 |
| **P45** | `ds.boll()` 的键名是 `mid / up / **low**`（不是直觉上的 `dn`） | 按直觉写 `b['dn']` 直接 KeyError | 统一用 `b['low']`，并在代码处加注 |
| **单位陷阱** | 腾讯快照的 `amount` 是**万元**，东财 `f48` 是**元** | 直接比对会报 99.99% 的**假冲突**（实测 12215 万 vs 122145773 元 是同一个数） | 交叉校验前先归一单位；`verify.py` 已内建 |
| **`%%` 转义** | 普通字符串 print 里写了 `%%` | 输出出现字面 `%%` | 只有 `%` 格式化时才需要 `%%` |

---

## v1.1.0 — 2026-09-17 · 评级口径 v3 + 准入清单

- **P27-P29**：`f184` 量纲 ×100 归一；内外盘改相对差 `|gap| ≥ 10%` 才计分；资金流样本 <3 天不判连续性
- **P30 评级闸门**：资金面 + 板块面同时缺失 → 输出「数据不足」，分数位 `—`
- **P33 板块级加分闸门**：个股跑输同业中位数时，板块级正分一律归零（负分照扣）
- **P34/P35 基准改版**：废弃"成分数最少板块"挑法，改为**双轨固定基准**（行业 = `f127`；题材 = 所属概念中主力净额最大者），**主判据取更不利者**
- **§1.5 下单前准入清单**、**§2.5 持仓处置与卖出 SOP**（含开盘三连否机械判据）
- **G5 费用模型重做**：拆分为佣金/最低佣金/印花税/过户费，新增 `buy_cost` / `sell_proceeds` / `break_even` / `stop_price`

## v1.0.0 — 2026-09-16 · 首个可用版本

- `ds.py` 零依赖数据源库（快照 / clist / ulist / fflow / 板块 / K线三源 / 情绪温度 / 全套指标）
- `scan_pool.py` 三口径扫描、`sector_compare.py` 板块横向对比、`mood.py` 情绪温度、`backtest.py` 形态回测、`report.py` 卡片式 HTML 报告
- `cfg.py` 个人参数外置（脱敏铁律）+ `config.example.json` 模板
- 开源：https://github.com/Y5H1ggs/niuma-stock-screener
